# Build outputs & intermediates (regenerable)
/course_en.md
/course_fr.md
.course_*.pdfsrc.md
player/site/

# Local audio backups — kept on disk, not committed
player/audio-v*/

# Deploy state — never commit (Netlify coordinates live elsewhere, kept private)
.netlify/

# Tooling / OS
__pycache__/
*.pyc
.DS_Store
.idea